# This is the default ackrc for ack 2.0

# There are four different ways to match
# is:  Match the filename exactly
# ext: Match the extension
# regex: Match against a Perl regular expression
# firstlinematch: Match the first 80 characters of the first line
#   of text against a Perl regular expression

# Directories to ignore
--ignore-directory=is,.bzr            # Bazaar
--ignore-directory=is,.cdv            # Codeville
--ignore-directory=is,~.dep           # Interface Builder
--ignore-directory=is,~.dot           # Interface Builder
--ignore-directory=is,~.nib           # Interface Builder
--ignore-directory=is,~.plst          # Interface Builder
--ignore-directory=is,.git            # Git
--ignore-directory=is,.hg             # Mercurial
--ignore-directory=is,.pc             # quilt
--ignore-directory=is,.svn            # Subversion
--ignore-directory=is,_MTN            # Monotone
--ignore-directory=is,blib            # Perl module building
--ignore-directory=is,CVS             # CVS
--ignore-directory=is,RCS             # RCS
--ignore-directory=is,SCCS            # SCCS
--ignore-directory=is,_darcs          # darcs
--ignore-directory=is,_sgbak          # Vault/Fortress
--ignore-directory=is,autom4te.cache  # autoconf
--ignore-directory=is,cover_db        # Devel::Cover
--ignore-directory=is,_build          # Module::Build

# Files to ignore
--ignore-file=ext,.bak             # Backup files
--ignore-file=match,/~$/           # Backup files
--ignore-file=match,/#.+#$/        # Emacs swap files
--ignore-file=match,/[._].*\.swp$/ # Vi(m) swap files
--ignore-file=match,/core\.\d+$/   # core dumps


# Filetypes defined
--type-add=perl,ext,pod
--type-add=perl,ext,pl
--type-add=perl,ext,pm
--type-add=perl,match,/perl.*/
--type-add=perl,firstlinematch,/perl/

--type-add=make,ext,mk
--type-add=make,match,/^Makefile$/i

--type-add=rake,is,Rakefile
